{
  "name": "sciencediscovery-website-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "postinstall": "ln -sfn \"$PWD/node_modules\" \"$PWD/../test/node_modules\"",
    "check:sync": "node ../test/sync-e2e.mjs --check",
    "test": "npm run check:sync && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "^22.0.0"
  }
}
